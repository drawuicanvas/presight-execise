packages:
  - 'apps/*'
allowBuilds:
  # Only used by the `sass` CLI's own --watch; Vite does its own file watching.
  '@parcel/watcher': false
  esbuild: true
engineStrict: true
injectWorkspacePackages: true
