/// <reference types="vite/client" />

interface ImportMetaEnv {
    /** Base URL of the API server, without a trailing slash. Defaults to http://localhost:3000. */
    readonly VITE_API_BASE_URL?: string
}
